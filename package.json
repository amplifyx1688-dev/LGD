{
  "name": "metricgram-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "metricgram-backend",
    "metricgram-shared-types"
  ],
  "scripts": {
    "install:all": "npm install",
    "build:shared": "npm run build -w @metricgram/shared-types",
    "dev:backend": "npm run dev -w metricgram-backend",
    "build:backend": "npm run build -w metricgram-backend"
  }
}
